namespace Enums
{
    public static class RingPositionTypes
    {
        public enum RingPositionType { not, left, middle, right};
    }
    
    public static class RingColors
    {
        public enum RingColor { not, yellow, pink, green, blue};
    }
}
